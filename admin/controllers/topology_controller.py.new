from flask import request, jsonify, Blueprint
from admin import db
from admin.models.topology import Topology
from datetime import datetime
import json

# Sample topologies data based on the topology.html implementation
sample_topologies = [
    {
        "id": 1,
        "title": "Point-to-Point",
        "description": "A point-to-point network topology consists of a direct connection between two devices.",
        "topology_type": "point-to-point",
        "difficulty": "easy",
        "base_score": 100,
        "time_bonus": 20,
        "perfect_match_bonus": 10,
        "is_active": True,
        "initial_config": {
            "devices": [],
            "connections": []
        },
        "expected_config": {
            "devices": [
                {"id": "pc1", "type": "pc", "name": "PC-1", "x": 0.3, "y": 0.5},
                {"id": "pc2", "type": "pc", "name": "PC-2", "x": 0.7, "y": 0.5}
            ],
            "connections": [
                {"source": "pc1", "target": "pc2"}
            ]
        },
        "scoring_metrics": {
            "time_efficiency": 10,
            "config_process": 25,
            "design_layout": 20,
            "completeness": 20,
            "correctness": 25
        },
        "device_requirements": {
            "pc": 2,
            "router": 0,
            "switch": 0
        }
    },
    {
        "id": 2,
        "title": "Star Network",
        "description": "A star network topology consists of one central device that's connected to multiple peripheral devices.",
        "topology_type": "star",
        "difficulty": "medium",
        "base_score": 150,
        "time_bonus": 30,
        "perfect_match_bonus": 15,
        "is_active": True,
        "initial_config": {
            "devices": [
                {"id": "sw1", "type": "switch", "name": "Core Switch", "x": 0.5, "y": 0.3}
            ],
            "connections": []
        },
        "expected_config": {
            "devices": [
                {"id": "sw1", "type": "switch", "name": "Core Switch", "x": 0.5, "y": 0.3},
                {"id": "pc1", "type": "pc", "name": "PC-1", "x": 0.3, "y": 0.6},
                {"id": "pc2", "type": "pc", "name": "PC-2", "x": 0.5, "y": 0.6},
                {"id": "pc3", "type": "pc", "name": "PC-3", "x": 0.7, "y": 0.6}
            ],
            "connections": [
                {"source": "sw1", "target": "pc1"},
                {"source": "sw1", "target": "pc2"},
                {"source": "sw1", "target": "pc3"}
            ]
        },
        "scoring_metrics": {
            "time_efficiency": 15,
            "config_process": 20,
            "design_layout": 25,
            "completeness": 20,
            "correctness": 20
        },
        "device_requirements": {
            "pc": 3,
            "router": 0,
            "switch": 1
        }
    },
    {
        "id": 3,
        "title": "Mesh Network",
        "description": "A mesh network topology consists of devices that are all connected to each other, creating multiple paths for data.",
        "topology_type": "mesh",
        "difficulty": "hard",
        "base_score": 200,
        "time_bonus": 40,
        "perfect_match_bonus": 20,
        "is_active": True,
        "initial_config": {
            "devices": [],
            "connections": []
        },
        "expected_config": {
            "devices": [
                {"id": "r1", "type": "router", "name": "Router 1", "x": 0.3, "y": 0.3},
                {"id": "r2", "type": "router", "name": "Router 2", "x": 0.7, "y": 0.3},
                {"id": "r3", "type": "router", "name": "Router 3", "x": 0.3, "y": 0.7},
                {"id": "r4", "type": "router", "name": "Router 4", "x": 0.7, "y": 0.7}
            ],
            "connections": [
                {"source": "r1", "target": "r2"},
                {"source": "r1", "target": "r3"},
                {"source": "r1", "target": "r4"},
                {"source": "r2", "target": "r3"},
                {"source": "r2", "target": "r4"},
                {"source": "r3", "target": "r4"}
            ]
        },
        "scoring_metrics": {
            "time_efficiency": 20,
            "config_process": 15,
            "design_layout": 30,
            "completeness": 15,
            "correctness": 20
        },
        "device_requirements": {
            "pc": 0,
            "router": 4,
            "switch": 0
        }
    },
    {
        "id": 4,
        "title": "Bus Network",
        "description": "A bus network topology consists of a main cable to which all devices are connected.",
        "topology_type": "bus",
        "difficulty": "easy",
        "base_score": 100,
        "time_bonus": 20,
        "perfect_match_bonus": 10,
        "is_active": True,
        "initial_config": {
            "devices": [],
            "connections": []
        },
        "expected_config": {
            "devices": [
                {"id": "pc1", "type": "pc", "name": "PC-1", "x": 0.2, "y": 0.5},
                {"id": "pc2", "type": "pc", "name": "PC-2", "x": 0.4, "y": 0.5},
                {"id": "pc3", "type": "pc", "name": "PC-3", "x": 0.6, "y": 0.5},
                {"id": "pc4", "type": "pc", "name": "PC-4", "x": 0.8, "y": 0.5}
            ],
            "connections": [
                {"source": "pc1", "target": "pc2"},
                {"source": "pc2", "target": "pc3"},
                {"source": "pc3", "target": "pc4"}
            ]
        },
        "scoring_metrics": {
            "time_efficiency": 10,
            "config_process": 25,
            "design_layout": 20,
            "completeness": 20,
            "correctness": 25
        },
        "device_requirements": {
            "pc": 4,
            "router": 0,
            "switch": 0
        }
    },
    {
        "id": 5,
        "title": "Ring Network",
        "description": "A ring network topology consists of devices connected in a circular pattern.",
        "topology_type": "ring",
        "difficulty": "medium",
        "base_score": 150,
        "time_bonus": 30,
        "perfect_match_bonus": 15,
        "is_active": True,
        "initial_config": {
            "devices": [],
            "connections": []
        },
        "expected_config": {
            "devices": [
                {"id": "pc1", "type": "pc", "name": "PC-1", "x": 0.5, "y": 0.2},
                {"id": "pc2", "type": "pc", "name": "PC-2", "x": 0.8, "y": 0.5},
                {"id": "pc3", "type": "pc", "name": "PC-3", "x": 0.5, "y": 0.8},
                {"id": "pc4", "type": "pc", "name": "PC-4", "x": 0.2, "y": 0.5}
            ],
            "connections": [
                {"source": "pc1", "target": "pc2"},
                {"source": "pc2", "target": "pc3"},
                {"source": "pc3", "target": "pc4"},
                {"source": "pc4", "target": "pc1"}
            ]
        },
        "scoring_metrics": {
            "time_efficiency": 15,
            "config_process": 20,
            "design_layout": 25,
            "completeness": 20,
            "correctness": 20
        },
        "device_requirements": {
            "pc": 4,
            "router": 0,
            "switch": 0
        }
    },
    {
        "id": 6,
        "title": "Tree Network",
        "description": "A tree network topology uses a hierarchical structure where devices are connected like branches on a tree.",
        "topology_type": "tree",
        "difficulty": "hard",
        "base_score": 200,
        "time_bonus": 40,
        "perfect_match_bonus": 20,
        "is_active": True,
        "initial_config": {
            "devices": [],
            "connections": []
        },
        "expected_config": {
            "devices": [
                {"id": "r1", "type": "router", "name": "Root Router", "x": 0.5, "y": 0.2},
                {"id": "sw1", "type": "switch", "name": "Switch 1", "x": 0.3, "y": 0.4},
                {"id": "sw2", "type": "switch", "name": "Switch 2", "x": 0.7, "y": 0.4},
                {"id": "pc1", "type": "pc", "name": "PC-1", "x": 0.2, "y": 0.6},
                {"id": "pc2", "type": "pc", "name": "PC-2", "x": 0.4, "y": 0.6},
                {"id": "pc3", "type": "pc", "name": "PC-3", "x": 0.6, "y": 0.6},
                {"id": "pc4", "type": "pc", "name": "PC-4", "x": 0.8, "y": 0.6}
            ],
            "connections": [
                {"source": "r1", "target": "sw1"},
                {"source": "r1", "target": "sw2"},
                {"source": "sw1", "target": "pc1"},
                {"source": "sw1", "target": "pc2"},
                {"source": "sw2", "target": "pc3"},
                {"source": "sw2", "target": "pc4"}
            ]
        },
        "scoring_metrics": {
            "time_efficiency": 20,
            "config_process": 15,
            "design_layout": 30,
            "completeness": 15,
            "correctness": 20
        },
        "device_requirements": {
            "pc": 4,
            "router": 1,
            "switch": 2
        }
    },
    {
        "id": 7,
        "title": "Hybrid Network",
        "description": "A hybrid network topology combines two or more different network topologies.",
        "topology_type": "hybrid",
        "difficulty": "hard",
        "base_score": 250,
        "time_bonus": 50,
        "perfect_match_bonus": 25,
        "is_active": True,
        "initial_config": {
            "devices": [],
            "connections": []
        },
        "expected_config": {
            "devices": [
                {"id": "r1", "type": "router", "name": "Core Router", "x": 0.5, "y": 0.2},
                {"id": "sw1", "type": "switch", "name": "Switch 1", "x": 0.3, "y": 0.4},
                {"id": "sw2", "type": "switch", "name": "Switch 2", "x": 0.7, "y": 0.4},
                {"id": "pc1", "type": "pc", "name": "PC-1", "x": 0.2, "y": 0.6},
                {"id": "pc2", "type": "pc", "name": "PC-2", "x": 0.4, "y": 0.6},
                {"id": "pc3", "type": "pc", "name": "PC-3", "x": 0.6, "y": 0.6},
                {"id": "pc4", "type": "pc", "name": "PC-4", "x": 0.8, "y": 0.6}
            ],
            "connections": [
                {"source": "r1", "target": "sw1"},
                {"source": "r1", "target": "sw2"},
                {"source": "sw1", "target": "pc1"},
                {"source": "sw1", "target": "pc2"},
                {"source": "sw2", "target": "pc3"},
                {"source": "sw2", "target": "pc4"},
                {"source": "pc1", "target": "pc2"},
                {"source": "pc3", "target": "pc4"}
            ]
        },
        "scoring_metrics": {
            "time_efficiency": 25,
            "config_process": 15,
            "design_layout": 25,
            "completeness": 15,
            "correctness": 20
        },
        "device_requirements": {
            "pc": 4,
            "router": 1,
            "switch": 2
        }
    }
]

class TopologyController:
    
    def get_all_topologies(self):
        """Get all topology challenges"""
        try:
            topologies = Topology.query.all()
            # If no topologies are found, return sample data
            if not topologies:
                print("No topologies found in database, returning sample data")
                return jsonify(sample_topologies), 200
            return jsonify([topology.to_dict() for topology in topologies]), 200
        except Exception as e:
            print(f"Error getting topologies: {str(e)}")
            # Return sample data even if there's an error
            return jsonify(sample_topologies), 200
    
    def get_topology(self, topology_id):
        """Get a specific topology challenge by ID"""
        try:
            topology = Topology.query.get(topology_id)
            if topology:
                return jsonify(topology.to_dict()), 200
            else:
                # Return a sample topology if not found
                for sample in sample_topologies:
                    if sample["id"] == topology_id:
                        print(f"DEBUG: Returning sample topology for ID {topology_id}")
                        return jsonify(sample), 200
                # If specific ID not found in samples, return the first sample
                print(f"DEBUG: Topology ID {topology_id} not found, returning default sample")
                return jsonify(sample_topologies[0]), 200
        except Exception as e:
            print(f"Error getting topology {topology_id}: {str(e)}")
            # Return first sample topology on error
            return jsonify(sample_topologies[0]), 200
    
    def create_topology(self):
        """Create a new topology challenge"""
        data = request.json
        
        if not data or not all(k in data for k in ['title', 'description', 'expected_config']):
            return jsonify({"error": "Missing required fields"}), 400
        
        try:
            # Create new topology object
            new_topology = Topology(
                title=data['title'],
                description=data['description'],
                difficulty=data.get('difficulty', 'medium'),
                topology_type=data.get('topology_type', 'point-to-point'),
                base_score=data.get('base_score', 10),
                time_bonus=data.get('time_bonus', 0),
                perfect_match_bonus=data.get('perfect_match_bonus', 5),
                is_active=data.get('is_active', True)
            )
            
            # Set configuration fields
            if 'initial_config' in data:
                new_topology.initial_config = data['initial_config']
            
            new_topology.expected_config = data['expected_config']
            
            # Set scoring metrics and device requirements if provided
            if 'scoring_metrics' in data:
                new_topology.scoring_metrics = data['scoring_metrics']
                
            if 'device_requirements' in data:
                new_topology.device_requirements = data['device_requirements']
            
            # Save to database
            db.session.add(new_topology)
            db.session.commit()
            
            return jsonify({"message": "Topology created successfully", "id": new_topology.id}), 201
        
        except Exception as e:
            db.session.rollback()
            return jsonify({"error": str(e)}), 500
    
    def update_topology(self, topology_id):
        """Update an existing topology challenge"""
        topology = Topology.query.get_or_404(topology_id)
        data = request.json
        
        if not data:
            return jsonify({"error": "No data provided"}), 400
        
        try:
            # Update basic fields
            if 'title' in data:
                topology.title = data['title']
            
            if 'description' in data:
                topology.description = data['description']
            
            if 'difficulty' in data:
                topology.difficulty = data['difficulty']
                
            if 'topology_type' in data:
                topology.topology_type = data['topology_type']
                
            if 'base_score' in data:
                topology.base_score = data['base_score']
                
            if 'time_bonus' in data:
                topology.time_bonus = data['time_bonus']
                
            if 'perfect_match_bonus' in data:
                topology.perfect_match_bonus = data['perfect_match_bonus']
                
            if 'is_active' in data:
                topology.is_active = data['is_active']
                
            # Update configuration fields
            if 'initial_config' in data:
                topology.initial_config = data['initial_config']
            
            if 'expected_config' in data:
                topology.expected_config = data['expected_config']
                
            # Update scoring metrics and device requirements if provided
            if 'scoring_metrics' in data:
                topology.scoring_metrics = data['scoring_metrics']
                
            if 'device_requirements' in data:
                topology.device_requirements = data['device_requirements']
            
            # Update the updated_at timestamp
            topology.updated_at = datetime.utcnow()
            
            # Save to database
            db.session.commit()
            
            return jsonify({"message": "Topology updated successfully"}), 200
        
        except Exception as e:
            db.session.rollback()
            return jsonify({"error": str(e)}), 500
    
    def delete_topology(self, topology_id):
        """Delete a topology challenge"""
        topology = Topology.query.get_or_404(topology_id)
        
        try:
            db.session.delete(topology)
            db.session.commit()
            return jsonify({"message": "Topology deleted successfully"}), 200
        except Exception as e:
            db.session.rollback()
            return jsonify({"error": str(e)}), 500
    
    def preview_topology(self, topology_id):
        """Get a topology in a format suitable for previewing"""
        try:
            topology = Topology.query.get(topology_id)
            if topology:
                # Return the topology with both configuration fields for the preview
                preview_data = topology.to_dict()
                return jsonify(preview_data), 200
            else:
                # Return a sample topology if not found
                for sample in sample_topologies:
                    if sample["id"] == topology_id:
                        print(f"Returning sample topology preview for ID {topology_id}")
                        return jsonify(sample), 200
                # If specific ID not found in samples, return the first sample
                print(f"Topology ID {topology_id} not found for preview, returning default sample")
                return jsonify(sample_topologies[0]), 200
        except Exception as e:
            print(f"Error getting topology preview {topology_id}: {str(e)}")
            # Return first sample topology on error
            return jsonify(sample_topologies[0]), 200
    
    def toggle_active_status(self, topology_id):
        """Toggle the active status of a topology challenge"""
        topology = Topology.query.get_or_404(topology_id)
        
        try:
            # Toggle the is_active field
            topology.is_active = not topology.is_active
            
            # Update the updated_at timestamp
            topology.updated_at = datetime.utcnow()
            
            # Save to database
            db.session.commit()
            
            return jsonify({
                "message": f"Topology is now {'active' if topology.is_active else 'inactive'}",
                "is_active": topology.is_active
            }), 200
        
        except Exception as e:
            db.session.rollback()
            return jsonify({"error": str(e)}), 500
